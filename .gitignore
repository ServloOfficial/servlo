build/
dist/
*.tmp
.venv/
.idea/
/servlo
servlo-frameworks/
servlo-services/
.claude/*
!.claude/skills/
# Committed on purpose: CLAUDE.md section 6 forbids agent attribution trailers,
# and a setting that travels with the repository enforces that for everyone
# rather than leaving it as a rule each person has to remember.
!.claude/settings.json
site/
docs/node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/
docs/public/install.sh
docs/public/demo/
docs/public/icons/icon.svg

# The proof sheet is a check, not a deliverable: regenerate it, do not commit it.
brand/preview.png
